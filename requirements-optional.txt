# Optional components. Install only the groups you intend to configure.
# LiteLLM gateway
litellm[proxy]

# Semantic Windows UI Automation
pywinauto

# Fully local voice fallback (large download; browser voice works without these)
faster-whisper
silero-vad
onnxruntime
piper-tts
# Microphone/speaker enumeration. Without it the device list reports
# unconfigured; nothing else in SAM uses it.
sounddevice

# Tray icon for the desktop program (install-desktop.ps1 installs it).
pystray
