fastapi>=0.116,<1
httpx>=0.28,<1
playwright>=1.52,<2
pydantic>=2.10,<3
python-dotenv>=1.0,<2
python-multipart>=0.0.9,<1
uvicorn[standard]>=0.35,<1
# Offline Windows OCR (price-axis calibration) and UI Automation.
winsdk>=1.0.0b10; sys_platform == 'win32'
pywinauto>=0.6.9; sys_platform == 'win32'
numpy>=1.26
# Fully local voice is optional and lives in requirements-optional.txt, which
# is what `setup.ps1 -InstallLocalVoice` installs. SAM starts without it:
# browser voice still works, and the capability reports itself unconfigured
# rather than pretending. Declaring it here as required was the only place in
# the repository that said so, and it made a default install fail its own
# voice tests.
