[pytest]
testpaths = tests
norecursedirs = .venv work data tools frontend
# The shared Windows temp root can be ACL-locked by another account; keep
# pytest's scratch space inside the project so the suite always runs.
addopts = --basetemp=work/pytest-tmp
